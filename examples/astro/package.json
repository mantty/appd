{
  "name": "appd-example-astro",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "appd:macos": "astro build && ../../target/release/appd build macos --project . --config dist/server/wrangler.json",
    "appd:ios-simulator": "astro build && ../../target/release/appd build ios-simulator --project . --config dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.8",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.5",
    "tailwindcss": "^4.2.2"
  }
}
